_schema-version: "3.3"
ID: scn-badges
version: 0.2.4

modules:
  - name: scn-badges-srv
    type: nodejs
    path: srv
    provides:
      - name: srv-api # required by consumers of services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    build-parameters:
      ignore: ["/node_modules"]
