_schema-version: "3.3"
ID: scn-badges
version: 1.1.6

modules:
  - name: scn-badges-srv
    type: nodejs
    path: srv
    provides:
      - name: srv-api # required by consumers of services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    build-parameters:
      ignore: ["/node_modules"]
